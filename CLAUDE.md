# CLAUDE.md

## Project Overview
UAfricas — plateforme panafricaine de développement durable. Monorepo :
- **`uafricas_frontend/`** — Nuxt 4 (Vue 3 SSR) + TypeScript + Tailwind CSS v4 + daisyUI v5 + Pinia
- **`uafricas_backend/`** — Rust (Edition 2024) + Actix-Web 4

## Dev Commands

| Commande | Description |
|----------|-------------|
| `pnpm dev` | Frontend dev (port 3000) |
| `pnpm build / generate / preview` | Build prod / SSG / preview |
| `RUST_LOG=info cargo run` | Backend (port 8082, recompile auto) |
| `kill $(lsof -i :8082 -t) 2>/dev/null; RUST_LOG=info cargo run` | Restart backend propre |
| `docker compose up -d / down / down -v` | PostgreSQL + Adminer + LiveKit |

> Toujours tuer l'ancien processus avant relancer le backend (port 8082 occupé sinon).

**BDD** : PostgreSQL `localhost:5432` (user: `uafricas`, db: `africans_db`). Adminer: `http://localhost:8088`. Schema SQL auto-init via `docker-init.sh`.

**No linting, testing, or CI/CD configured yet.**

## Backend Env Vars
Required: `DATABASE_URL`, `JWT_SECRET`, `SMTP_HOST`, `SMTP_USERNAME`, `SMTP_PASSWORD`.
Defaults: `HOST` (127.0.0.1), `PORT` (8080), `UPLOAD_DIR` (./uploads), `FRONTEND_URL` (http://localhost:3000), `JWT_EXPIRATION_MINUTES` (15), `REFRESH_EXPIRATION_DAYS` (7), `SMTP_PORT` (587), `SMTP_FROM_EMAIL` (=SMTP_USERNAME), `SMTP_FROM_NAME` (UAfricas), `EMAIL_VERIFICATION_EXPIRATION_HOURS` (24), `LIVEKIT_URL` (ws://localhost:7880), `LIVEKIT_API_KEY` (devkey), `LIVEKIT_API_SECRET` (secret).

## Architecture Frontend

- **Routing** : Nuxt 4 file-based, `app/pages/`, `[id].vue` dynamique. Layouts: `default.vue` (NavBar+Footer), `auth.vue` (minimal).
- **Composants** : `app/components/` feature-based (marche/, experts/, evenements/, forums/, universite/, social/, afrolang/, common/, layout/). PascalCase, pattern Hero/Card/Filters/Modal.
- **Store** : Pinia unique `app/stores/user.ts` — Utilisateur (id, nom, prenom, email, roles, etat), accessToken (mémoire), refreshToken (localStorage). Getters: fullName, displayName, isAdmin, isValidated.
- **Icons** : FontAwesome global via `app/plugins/fontawesome.ts`, ajouter dans `library.add()`.
- **Mocks** : `app/mocks/` — interfaces TS + données + helpers async. **Source de vérité = schéma SQL**, adapter interfaces au SQL.

### Composables (`app/composables/`)
Pattern : API via $fetch, filtres/pagination, mapping DB↔frontend. Un composable par domaine (public et admin). Nommage: `useXxx` (public), `useAdminXxx` (admin, base `useAdmin` avec adminFetch/listerPagine/pagination/sort). Globaux : `useAuth`, `useAOS`.

## Architecture Backend

Actix-Web 4 modulaire : `config.rs`, `errors.rs`, `models/`, `handlers/`, `routes.rs`, `jwt.rs`. Pattern: `ApiResponse<T>` wrapper, `COLONNES` const, `FromRow` structs, DTO Response séparés. Routes complètes : voir `src/routes.rs`. Un fichier handler/model par domaine ; admin sous `src/handlers/admin/` et `src/models/admin/`.

**Domaines principaux** : Auth (JWT/refresh), Public (livres, centres-culturels, codimoi, evenements, gouvernance, marché, television, vidafrica), Admin (IAM, référentiels, programmes, marché, innovation, culture, gouvernance, médias, audit, profils pays, vidafrica, bibliothèques humaines, expertise, afrolang salles), Social (amitié + messagerie, schéma `social`).

**Services** : `src/services/` — `audit.rs` (`log_action` non-bloquant, ~100 mutations instrumentées), `messagerie_sse.rs`, `livekit_moderation.rs`, validations diverses.

### Auth
JWT HS256 access (15min) + refresh (7j, SHA-256 hashé dans `iam.refresh_token`). Bcrypt cost 12. Vérification email: inscription → `etat='en_attente'` → token SHA-256 (`iam.token_verification_email`, 24h) → SMTP STARTTLS 587 → clic = `etat='actif'` + `email_verifie=true` + auto-login. Modules: `jwt.rs`, `email.rs`.

### Deps backend
actix-web 4, actix-cors, actix-multipart, actix-files, sqlx (PostgreSQL), uuid, chrono, dotenvy, serde, sanitize-filename, bcrypt, jsonwebtoken, sha2, rand, livekit-api, lettre, futures-util, tokio, image.

### Upload
Stockage local sous `./uploads/` (`couvertures/`, `documents/`, `videos/`, `vignettes/`, `afrolang/ressources/`, `opportunite-afrique/photos/`, `marketplace/annonces/`), servi via actix-files sur `/uploads/`.

### Database
PostgreSQL 16 Docker. Schema SQL: `uafricas_backend/doc/bd/schema.sql` (orchestrateur via `\ir` dans `schemas/`). Init auto via `docker-init.sh`.

Schemas bounded-context : `shared`, `iam`, `marketplace`, `exchange`, `innovation`, `culture`, `afrolang`, `media_content`, `governance`, `country_profile`, `arbre_genealogique`, `social`.

Conventions BDD: UUID v4 PKs, soft deletion (`deleted_at`), TIMESTAMPTZ, snake_case français, enums PostgreSQL, full-text search (TSVECTOR + GIN).

## Conventions
- **Langue** : code, commentaires, variables, UI en **français** avec accents (é, è, ê, à, ç, ù) obligatoires.
- **Nommage fichiers/dossiers** : PAS d'accents ni de caractères spéciaux (encodage SSH/Docker en prod). Uniquement `[a-z0-9_-]`.
- **Pages publiques = Tailwind v4 pur** ; daisyUI réservé au back-office admin (Principe VI).
- **Tailwind v4 + daisyUI v5** : CSS-first via `@theme` dans `app/assets/css/main.css` (pas de `tailwind.config.ts`). Plugin Vite `@tailwindcss/vite`. daisyUI via `@plugin "daisyui"`. Couleurs: `custom-chocolat` (#A54A1C), `custom-green` (#228B22), `custom-gray`. Fonts: Oswald (titres), Open Sans (body). Spacing jusqu'à `164` (64rem).
- **Migration v3 → v4** : projet migré ; des résidus v3 peuvent subsister. Si détectés, migrer vers v4 (sous-agent dédié).
- **Mock pattern** : interfaces TS + tableau + helpers (getById, filter, formulaire vide) + async delay().

## Infrastructure

- **Docker Dev** : `docker-compose.yml` — postgres, adminer, livekit + volume pgdata. LiveKit: 7880 (WS), 7881 (HTTP), 7882 (TCP), 50000-50100/udp. Config `livekit.yaml`. `.env` gitignored.
- **Docker Prod** : `docker-compose.prod.yml` — 7 services (postgres, backend, frontend, nginx, livekit, **coturn**, adminer optionnel) + 2 volumes (pgdata, uploads_data). Nginx HTTPS Let's Encrypt, reverse proxy frontend:3000 + backend:8080, rate limit API 30r/s + auth 5r/s, gzip, HSTS. Domaine `www.africans-world.org`.
- **TURN (appels P2P PeerJS)** : service **coturn** (`network_mode: host`, port **3479** UDP/TCP — 3478 réservé à LiveKit, relais `49160-49200/udp`, credentials long-terme `TURN_USERNAME`/`TURN_PASSWORD`). **Indispensable en prod** : sans relais TURN, deux pairs derrière des NAT symétriques n'échangent pas leur flux vidéo (vidéo distante noire). Le frontend reçoit `NUXT_PUBLIC_ICE_SERVERS` (STUN public + TURN udp/tcp) **au runtime** (aucun rebuild Nuxt requis). Variables dans le `.env` prod (gitignored) : `TURN_PUBLIC_IP` (IP publique VPS, partagée par `--external-ip` coturn et l'URL ICE frontend), `TURN_USERNAME`, `TURN_PASSWORD`. Voir `.env.production.example`.
- **Déploiement** : `deploy.sh` → VPS `root@161.97.92.63:/opt/uafricas` via SSH+Docker. Commandes: `setup`, `deploy`, `update`, `rebuild`, `status`, `logs [svc]`, `restart [svc]`, `stop`, `ssl`, `backup`, `connect`. Migrations BD manuelles via SSH+psql.

## LSP & Diagnostics
- **rust-analyzer** + **Volar** (VS Code). Utiliser `getDiagnostics` après chaque modification de fichier.

## Parallel Sub-agents
Recherche frontend + backend simultanée, exploration multi-fichiers en parallèle, tests/vérifications en parallèle après modifications. Avant nouveau composant : vérifier si similaire existe dans `app/components/`.

## Auto-maintenance
Mettre à jour ce fichier lors de : ajout/suppression service Docker ou dépendance majeure, nouveau composable/store/module, nouvel endpoint API ou schema BDD, changement commandes dev, conventions, CI/CD.

## Test Users
- `test-admin@test.com` / `Test1234`
- `test-user@test.com` / `Test1234`

## Tech Stack par feature
Backend Rust Edition 2024 + Actix-Web 4 + sqlx (PostgreSQL) ; frontend Nuxt 4 / Vue 3 SSR + Pinia + Tailwind v4. Extensions notables : `pg_trgm` (matching arbres), LiveKit (afrolang temps réel + modération), SSE via `futures-util`/`tokio` (messagerie social), `image` crate (validation photos), `lettre` (SMTP), `@vue-flow/core` (visualisation arbre généalogique), `@excalidraw/excalidraw` (tableau blanc afrolang, MIT).

## Recent Changes (résumé)
Spécifications détaillées sous `specs/`. Historique des features livrées (consulter git log + specs pour le détail) :
- **télévision : télés (chaînes regroupant des programmes) + programme à la une en boucle** : sur `/tele`, les vidéos sont désormais **regroupées par « télé »** — une télé = une **chaîne TV** (`media_content.chaine_tv`), chaque vidéo = un **programme** télé (`media_content.programme_radio_tele`, `type='tele'`). Un programme peut être rattaché à une chaîne via **`chaine_id`** et, au sein d'une chaîne, **un seul** programme est marqué **`a_la_une`** : c'est lui qui **joue en boucle sur l'écran principal** (`<video loop>`) de la page. Migration idempotente `schemas/09d_media_content_programme_chaine.sql` : `programme_radio_tele` reçoit `chaine_id UUID` ([xref] `chaine_tv`) + `a_la_une BOOLEAN NOT NULL DEFAULT false`, index `idx_radio_tele_chaine` + **index unique partiel** `uq_programme_a_la_une_par_chaine` (`chaine_id WHERE a_la_une AND type='tele' AND deleted_at IS NULL` = une seule à la une par télé, filet de sécurité). Backend public (`models/television.rs` + `handlers/television.rs`) : `PROGRAMME_TELE_COLONNES` + `ProgrammeTeleRow`/`Response` exposent `chaine_id`/`chaine_nom` (JOIN `chaine_tv`)/`a_la_une` ; `lister_programmes_vedettes` filtre par `chaine` (param UUID, cast `::uuid`) et trie **`a_la_une DESC, created_at DESC`** ; `obtenir_programme_vedette` et `creer_programme_vedette` (contribution membre) propagent `chaine_id` (a_la_une **réservé au back-office**). **Back-office** (`models/admin/radio_tele.rs` + `handlers/admin/radio_tele.rs`) : `chaine_id`/`chaine_nom`/`a_la_une` ajoutés aux colonnes liste+détail (JOIN `chaine_tv ch`), `Creer`/`ModifierProgrammeMediaRequest` + `creer_programme_media`/`modifier_programme_media` gèrent les 2 champs ; **basculer `a_la_une=true`** retire d'abord le marqueur des autres programmes de la même chaîne (UPDATE de réinitialisation, chaîne effective = corps ou existante) avant l'écriture → respecte l'index unique. Frontend : `useTelevision` (types `ProgrammeTeleAPI`/`TvProgram` + `chaineId`/`chaineNom`/`aLaUne`, filtre `chaine`), page **`tele.vue` refondue** (Tailwind pur) : groupe `TeleGroupe` (chaîne + ses programmes + à la une, plus un groupe « Autres programmes » pour les orphelins), écran principal joue l'à la une de la télé active **en boucle** (`watch(heroVideoUrl)` → `load()`+`play()`), grille de **cartes télé** sélectionnables (compteur de programmes, badge LIVE), timeline + liste des programmes de la télé active (clic = lecture). Formulaires admin `radio-tele/{create,[id]}.vue` : sélecteur **Télé (chaîne)** (via `useTelevision.listerChaines`) + case **À la une** (désactivée sans chaîne), visibles seulement si `type='tele'` ; liste admin (`radio-tele/index.vue`) : colonne « Télé / À la une » (badge `faStar`). Types admin `AdminProgrammeMedia*`/`CreerProgrammeMediaForm` étendus.
- **facultés & écoles partenaires (INUDA) : back-office CRUD** : le domaine `exchange` (facultés `exchange.faculte` + écoles `exchange.ecole_partenaire`, lecture publique `/api/facultes` déjà existante) reçoit une **couche admin complète** pour créer/modifier/supprimer. Aucune migration BD (tables déjà présentes). Permission utilisée : **`programme`** (domaine académique/exchange). Backend : `models/admin/faculte.rs` (DTO liste/détail + `Creer`/`Modifier` Request pour écoles et facultés, query params) + `handlers/admin/facultes.rs`. Endpoints (`/api/admin`, JWT admin) : **écoles** `GET|POST /ecoles-partenaires`, `GET|PUT|DELETE /ecoles-partenaires/{id}` (DELETE = soft via `actif=false`, **bloqué si des facultés actives y sont rattachées** → 409) ; **facultés** `GET|POST /facultes`, `GET|PUT|DELETE /facultes/{id}` (DELETE = soft via `deleted_at`). Helpers : `valider_type_ecole` (publique/privee), `valider_statut` (active/inactive), `generer_slug` (titre + 8 car. UUID, unicité garantie), `nettoyer_liste` (TEXT[] trim/filtre vides), `texte_optionnel`, `verifier_ecole_existe`. UPDATE faculté = **remplacement complet** (≈20 champs dont 6 tableaux PG bindés en `Vec<String>`), `nombre_inscrits_*` via `COALESCE`. Audit `exchange/faculte` + `exchange/ecole_partenaire` (`CREATE`/`UPDATE`/`DELETE`). Frontend admin (daisyUI) : types `AdminFaculte`/`AdminFaculteDetail`/`CreerFaculteForm` + `AdminEcolePartenaire`/`...Detail`/`CreerEcolePartenaireForm`, composables `useAdminFacultes` + `useAdminEcolesPartenaires` (`listerToutes` non-paginé pour alimenter le sélecteur d'écoles), pages `admin/facultes/{index,create,[id]}.vue` + `admin/ecoles-partenaires/{index,create,[id]}.vue`, composant partagé **`AdminFaculteForm.vue`** (formulaire riche : identité, image+logo via `OpportuniteAfriqueImageUploadField`, domaines/programmes/langues/points-forts en **textarea 1-ligne=1-élément** via proxy `computed` get/set, sélecteur d'école, frais/bourses, statut). Sélecteur de territoire via `useCentresCulturels.listerPays` (`/api/pays`). Entrées sidebar sous « Programmes d'échange » (icônes `faBuildingColumns`/`faSchool`). La page publique `/universite/facultes` reflète automatiquement les facultés `statut='active'`.
- **vidafrica : réactions (like/dislike) & partage** : sur `/vidafrica/[slug]`, tout membre connecté peut **aimer / ne pas aimer** une vidéo (réaction unique basculable, modèle `iam.biblio_reaction`) et la **partager** sur le mur `/publications` avec une légende facultative (modèle `social.partage_profil` / `country_profile.partage_fiche`). Migration idempotente `schemas/27d_vidafrica_reactions_partages.sql` : `media_content.video_reaction` (`UNIQUE(video_id, utilisateur_id)`, like/dislike) + `media_content.partage_video` (soft-delete, `legende` ≤500). **Aucun compteur dénormalisé** : totaux calculés à la lecture. Backend : handler **membre** `vidafrica_contribution` (JWT via `utilisateur_courant`) — `POST /api/vidafrica/videos/{id}/reaction` (toggle INSERT/UPDATE/DELETE, vidéo `publie` requise) + `POST /api/vidafrica/videos/{id}/partage` ; handler **public** `vidafrica` — `obtenir_video_publique` enrichi (`nombre_likes`/`nombre_dislikes`/`nombre_partages`/`ma_reaction` via JWT optionnel extrait du header) + `GET /api/vidafrica/videos/partages` (mur public paginé, **déclaré avant `/videos/{slug}`**, `PARTAGE_VIDEO_SELECT` pub réutilisé par le handler membre). Modèles dans `models/vidafrica.rs` (`VideoReactionRequest`/`Response`, `PartageVideo*` + `From<Row>`). Frontend : `useVidafrica` étendu (`authHeaders`, `chargerVideo` envoie le JWT, `reagirVideo`/`partagerVideo`/`listerPartagesVideos` + types `ReactionVideoEtat`/`PartageVideoAPI`), composants Tailwind pur `VidafricaReactionsBar` (barre aimer/ne pas aimer/partager, état local resynchronisé via `watch`) + `VidafricaPartagerModal` (aperçu vidéo + légende) branchés dans `vidafrica/[slug].vue`. **Mur `/publications`** : nouvelle source `video_partage` (composant `publications/VideoPartageCard`, thème violet/fuchsia) intégrée au feed (`listerPartagesVideos` dans le `Promise.allSettled`, type union `PublicationVideoPartage`, `STYLES_PAR_TYPE`/filtre/compteurs/gardes de narrowing). Icônes déjà présentes (`faThumbsUp/Down`, `faShareNodes`, `faVideo`, `faPlay`).
- **vidafrica : infos de proposition (territoires, décharge de droits, auteur réel)** : sur `/vidafrica` (modale `VidafricaProposerVideoModal`), une vidéo proposée par un membre porte désormais **des territoires** (multi-sélection depuis `PAYS_AFRICAINS`, chips supprimables), une **mention de décharge** obligatoire (« Je ne suis pas l'auteur de cette chanson et ne revendique aucun droit à ce sujet », case à cocher requise) et un champ **auteur réel** (facultatif). Migration idempotente `schemas/27c_vidafrica_proposition_infos.sql` : `media_content.video` reçoit `territoires TEXT[] NOT NULL DEFAULT '{}'`, `decharge_droits BOOLEAN NOT NULL DEFAULT false`, `auteur_reel TEXT` (rétrocompat). Backend public (`handlers/vidafrica_contribution.rs::proposer_video`) : parsing multipart (`territoires` **champ répété**, `decharge_droits` accepté si `true`/`1`/`on`, `auteur_reel`), **décharge exigée** (sinon 400), INSERT enrichi ; `models/vidafrica.rs` + `handlers/vidafrica.rs::obtenir_video_publique` exposent `territoires`/`auteur_reel` dans le détail public. **Back-office** (`models/admin/vidafrica.rs` + `handlers/admin/vidafrica.rs`) : champs propagés à `ADMIN_VIDEO_DETAIL_COLONNES` + `AdminVideoDetailRow`/`Response` + `to_response` ; `creer_video` parse/INSERT les 3 champs ; `modifier_video` — comme `territoires` (TEXT[]) et `decharge_droits` (BOOLEAN) ne passent pas par le builder dynamique String, ils sont appliqués en **UPDATEs typés séparés** (marqueur `territoires_modifies` pour autoriser le vidage complet de la liste), `auteur_reel` reste dans le builder String. Frontend : type `Video` (mocks) + `VideoAfrica` (`useVidafrica`, `territoires`/`auteurReel`) étendus ; modale publique (Tailwind pur) et pages admin `vidafrica/create.vue` + `[id].vue` (daisyUI : sélecteur territoire + badges, input auteur réel, checkbox décharge) ; affichage sur la page détail publique `vidafrica/[slug].vue` (badges territoires, auteur réel, mention de décharge en italique). Icônes `faLocationDot`/`faUser`/`faXmark` déjà présentes.
- **biblio humaine : interactions sur la page profil** : sur `/profil/[id]` (onglet « Bibliothèque Humaine »), tout membre connecté peut **aimer / ne pas aimer** (réaction unique basculable), **commenter** (liste plate, livre d'or) et **recommander** la biblio humaine avec un témoignage facultatif (endossement, une recommandation par membre, modifiable/retirable). La cible est l'**utilisateur-biblio** (`utilisateur_id`, comme `obtenir_biblio`). Trois tables `iam` (migration idempotente `schemas/04g_iam_biblio_interactions.sql`) : `biblio_reaction` (`UNIQUE(utilisateur_id, auteur_id)`, like/dislike, toggle façon codimoi), `biblio_commentaire` (soft-delete, `contenu` 1–2000), `biblio_recommandation` (`UNIQUE(utilisateur_id, auteur_id)`, `message` ≤1000, upsert `ON CONFLICT`). **Aucun compteur dénormalisé** : totaux calculés à la lecture. Backend (handler **public** `bibliotheques_humaines`, JWT extrait du header) : `obtenir_biblio` lit le JWT en option et enrichit `BiblioHumaineResponse` (`nombreLikes`/`nombreDislikes`/`nombreRecommandations`/`nombreCommentaires`/`maReaction`/`aRecommande`) ; nouveaux endpoints `POST /{id}/reaction`, `GET|POST /{id}/commentaires`, `DELETE /{id}/commentaires/{commentaire_id}` (son propre, soft-delete), `POST|DELETE /{id}/recommandation`, `GET /{id}/recommandations`. Helper `verifier_biblio_valide` + interdiction d'agir sur son propre profil. Frontend (Tailwind pur) : `useBibliothequeHumaine` étendu (`reagirBiblio`, `listerCommentairesBiblio`/`ajouterCommentaireBiblio`/`supprimerCommentaireBiblio`, `recommanderBiblio`/`retirerRecommandationBiblio`/`listerRecommandationsBiblio`, `obtenirBiblio` envoie le JWT) + composant auto-importé `BibliothequeInteractions` (barre aimer/ne pas aimer/recommander, liste de témoignages, fil de commentaires + formulaire, modale de recommandation) branché dans l'onglet biblio de `profil/[id].vue` (`:peut-interagir="peutNoter"`). Icônes déjà présentes (`faAward`, `faPaperPlane`, `faTrash`, `faThumbsUp/Down`, `faComment`).
- **événements : champs détail + type d'organisateur** : le formulaire public de proposition d'événement (`/evenements/liste`, `EvenementModal`) renseigne désormais tous les champs affichés sur la page détail — **adresse précise** (présentiel/hybride), **lien de participation** (en ligne/hybride), **nombre total de places** — et un **contact de l'organisateur**. L'organisateur **choisit** s'il publie **en son nom propre** (`personnel`) ou **au nom d'une organisation** (`organisation`) — distinction **importante pour les statistiques**. Migration idempotente `schemas/09c_media_content_evenement_contact.sql` : enum `media_content.type_organisateur AS ENUM ('personnel','organisation')` + colonne `type_organisateur NOT NULL DEFAULT 'personnel'` + `contact_nom`/`contact_email`/`contact_telephone`/`contact_site_web` (nullables, rétrocompat). Quand `organisation`, `contact_nom` porte le **nom de l'organisation** (requis, validé backend). Backend (`models/evenement.rs` + `handlers/evenements.rs`, handler **public** existant) : champs ajoutés à `EVENEMENT_COLONNES` (cast `type_organisateur::text`) + `EvenementRow` + `EvenementDetailResponse`, parsing multipart (`adresse` enfin insérée, auparavant codée `NULL`), validation `TYPES_ORGANISATEUR_VALIDES`, INSERT avec cast `$n::media_content.type_organisateur`, helper `texte_optionnel`. Frontend (Tailwind pur) : `useEvenements` (type `EvenementDetailAPI` + signature `creerEvenement` étendus, FormData), `EvenementModal` (champs conditionnels selon le type d'événement + radios nom propre/organisation, nom d'organisation requis si `organisation`), page `[id].vue` (badge « Publié en nom propre » / « Au nom de X » dans le bloc Organisateur + bloc « Contact de l'organisateur »). **Back-office** (handler/modèle admin JSON `admin/evenement`) : champs propagés à `ADMIN_EVENEMENT_DETAIL_COLONNES` (cast `type_organisateur::TEXT`) + `AdminEvenementDetailRow`/`Response` + `to_response` + `Creer`/`ModifierEvenementRequest` ; INSERT/UPDATE avec cast `::media_content.type_organisateur` + validation `TYPES_ORGANISATEUR_VALIDES` (nom d'organisation requis si `organisation`, vidé/`NULL` si `personnel`). Frontend admin (daisyUI) : types `AdminEvenementDetail`/`CreerEvenementAdminForm` étendus, section « Organisateur & contact » (radios + champs) dans `admin/evenements/create.vue` et `[id].vue`. **Gestion par l'organisateur (membre)** : onglet « Mes événements » sur `/mon-compte/profil` où l'auteur (`cree_par`) gère ses événements (tous états). Couche membre dans le handler **public** `evenements` (JWT, contrôle de propriété via `charger_mon_evenement`) : `GET /api/evenements/mes-evenements` (liste, déclaré **avant** `/{id}`), `PUT /api/evenements/{id}` (modif JSON `ModifierMonEvenementRequest` — champs partiels, `''`→NULL, format/territoire/type d'organisateur revalidés), `DELETE /api/evenements/{id}` (soft delete), `GET /api/evenements/{id}/inscrits` (liste des inscrits non annulés). `EvenementDetailResponse` enrichi de `etat` (brut) ; helper `construire_detail` + `parser_date`. Frontend (Tailwind pur) : `useEvenements` (types `InscritEvenement`/`ModifierMonEvenementPayload`, `listerMesEvenements`/`modifierMonEvenement`/`supprimerMonEvenement`/`listerInscritsMonEvenement`), composant `evenements/MesEvenements.vue` (cartes avec badge état, places/inscrits, modal d'édition complet, dépliage des inscrits, confirmation de suppression, **bouton « Démarrer le direct »** → `/evenements/{id}/direct` pour les événements en ligne/hybride publiés). Icônes `faCalendarPlus`/`faListCheck` ajoutées au plugin. **Couverture modifiable côté membre** : endpoint multipart `POST /api/evenements/{id}/couverture` (`changer_couverture_mon_evenement`, owner-checked, réutilise `sauvegarder_fichier`, renvoie la nouvelle URL) + `useEvenements.changerCouvertureMonEvenement(id, file)` ; le modal d'édition expose un champ fichier avec aperçu (upload après le PUT JSON). **Onglets `/mon-compte/profil` en dropdown** : la barre d'onglets (désormais 8) débordait/était masquée à droite → remplacée par un menu déroulant (bouton affichant l'onglet courant + liste, fermeture au clic-hors-zone) ; logique `tabMenuOuvert`/`ongletCourant`/`selectionnerOnglet`, icône `faChevronDown`.
- **profils : notation, partage & signalement** : sur `/profil/[id]`, trois interactions membre-à-membre ajoutées. **(1) Notation d'expert** : un membre connecté note un expert validé 1–5 étoiles (note seule, hors son propre profil, 1 note modifiable). Table `iam.note_expertise` (`UNIQUE(expertise_id, auteur_id)`, migration `04d_iam_note_expertise.sql`) ; `iam.expertise.rating` = moyenne recalculée à chaque vote. Endpoint `POST /api/experts/{id}/note` (upsert + recompute), `GET /api/experts/{id}` lit le JWT en option pour renseigner `maNote` (+ `nombreNotes` via subquery dans `EXPERT_COLONNES`). Frontend : `useExperts.noterExpert`, composant Tailwind pur `experts/NotationExpert.vue` (étoiles interactives) remplaçant la carte « Note » statique de l'onglet Expertise. **(2) Partage de profil** sur le mur `/publications` (calqué sur `country_profile.partage_fiche`) : table `social.partage_profil` (`profil_id`/`utilisateur_id`/`legende`/soft-delete, migration `32_social_partage_profil.sql`), `POST /api/utilisateurs/{id}/partages` + `GET /api/utilisateurs/partages` (public paginé, exclut profils non `actif`, JOIN aperçu expert/biblio/pays). Frontend : `useMembres.partagerProfil`/`listerPartagesProfils`, composants Tailwind pur `profil/PartagerProfilModal.vue` + `publications/ProfilPartageCard.vue`, nouveau type `profil_partage` intégré au feed `publications/index.vue` (filtre, style, chargement). **(3) Signalement de profil** (faux profils / arnaques, calqué sur `governance.factcheck_signalement`) : table `iam.signalement_profil` (`UNIQUE(utilisateur_id, signale_par)`, motif/description) + colonne `iam.utilisateur.nombre_signalements` (migration `04e_iam_signalement_profil.sql`), `POST /api/utilisateurs/{id}/signalement` (insert idempotent + recompte + **suspension auto** `etat='suspendu'` au-delà de `SEUIL_SIGNALEMENTS_PROFIL_SUSPENSION=20`, audit `SIGNALEMENT`/`SIGNALEMENT_SUSPENSION`). Frontend : `useMembres.signalerProfil` + composant `profil/SignalerProfilModal.vue` (sélecteur de motif). Les profils suspendus deviennent invisibles (`membres::obtenir_membre` filtre déjà `etat='actif'`). Backend : `models/profil_social.rs` + `handlers/profil_social.rs` (routes `/utilisateurs` : `/partages` déclaré avant `/{id}`). Pas encore de page admin de modération des signalements de profil (la suspension auto + l'audit couvrent le MVP).
- **arbre généalogique : assistant conversationnel** : sur `/arbre-genealogique/visualisation`, l'ancien wizard **modal** d'onboarding est remplacé par un **panneau « chatbot » docké à droite** qui construit l'arbre **progressivement** pendant que le graphe se dessine à gauche. Scénario guidé : **(1)** soi-même → **(2)** père puis mère → **(3)** parents des parents (remontée BFS, gate « Souhaitez-vous renseigner les parents de X ? » par ascendant, sauts « Je ne sais pas ») → **(4)** « Avez-vous des enfants ? » (boucle) → **(5)** petits-enfants (gate par enfant). Aucune modif backend (réutilise `creerPersonne` + `creerLien`, orientation parent→enfant `pere`/`mere`/`parent` selon le genre). Nouveau composable **`useAssistantArbre`** (machine à états : `messages`/`promptActif`/`enCours`/`erreur`/`termine`/`phaseLabel` + `demarrer`/`reinitialiser`/`soumettrePersonne`/`passer`/`repondre` ; rollback de la personne orpheline si le lien échoue ; callback `onArbreModifie`) + composant Tailwind pur **`AssistantConversationnel.vue`** (fil de discussion, mini-formulaire de saisie inline, boutons Oui/Non). Page `visualisation.vue` : layout 2 colonnes (arbre `flex-1` + assistant `w-[24rem]`, plein écran mobile), auto-ouverture quand l'arbre est vide, bouton flottant « Assistant », recadrage `fitView` à l'ouverture/fermeture + sur `@arbre-modifie`. **Clic sur une personne de l'arbre = flux contextuel dans le chat** (et non plus l'ancien panneau/wizard) : l'assistant ouvre un **menu** « ajouter un parent / un enfant / un(e) conjoint(e) / non c'est tout », collecte la personne puis crée le lien (orientation : parent→cible selon le genre saisi ; enfant = cible→nouveau selon le genre de la cible ; conjoint = `conjoint`), et repropose le menu jusqu'à « Terminé ». `useAssistantArbre` étendu (`demarrerContexte`/`choisirAction`, type `PromptMenu`, rôles `ctx_parent`/`ctx_enfant`/`ctx_conjoint`, reprise de l'onboarding s'il était interrompu, état `idle`) ; le composant reçoit le prop `cible` (`{rattId,label,genre,seq}` — `seq` incrémenté à chaque clic). `surClicNoeud` n'ouvre plus `PanneauPersonne`. Icône `faSeedling` ajoutée au plugin FontAwesome.
- **inscription aux programmations de centre culturel** : sur `/centres/{id}/programmations/{programmationId}`, le bouton mock « Je suis intéressé » est remplacé par une **vraie inscription** (utilisateur connecté). Nouvelle table `culture.programmation_inscription` (calquée sur `media_content.evenement_inscription` : `UNIQUE(programmation_id, utilisateur_id)`, statut `inscrit`/`confirme`/`annule`/`present`/`absent`, désinscription = `statut='annule'`), migration idempotente `schemas/08f_culture_programmation_inscription.sql`. Backend (handler **public** `centres_culturels`, JWT extrait du header comme `evenements.rs`) : `POST .../{centre_id}/programmations/{id}/inscription` (ON CONFLICT upsert + **contrôle de capacité** vs `nombre_places` → 409 si complet), `DELETE .../inscription` (annule). `ProgrammationResponse` enrichi de `nombre_inscrits` (compté) + `est_inscrit` (calculé via JWT optionnel dans `obtenir_programmation`). Frontend : `useCentresCulturels` (`authHeaders`, `inscrireProgrammation`/`desinscrireProgrammation`, `obtenirProgrammation` envoie le JWT), page programmation = bouton bascule **S'inscrire/Se désinscrire** + badge « inscrit » + places restantes ; `est_inscrit` rafraîchi **côté client** au montage (le token n'existe pas au rendu SSR). Icône `faUserMinus` ajoutée au plugin FontAwesome. **Back-office** : `GET /api/admin/programmations/{id}/inscriptions` (perm `culture`/`voir`, JOIN `iam.utilisateur`, exclut `annule`, modèle `AdminProgInscriptionResponse`) listé dans une carte « Inscrits » sur la page admin d'édition de programmation (`useAdminProgrammations.listerInscriptions`, type `AdminProgrammationInscription`). **Infos d'inscription** : l'inscription collecte désormais **nom, prénom, pays, lieu de résidence, titre** (colonnes nullables sur `programmation_inscription`, migration idempotente `schemas/08g_culture_inscription_infos.sql`). Backend : `InscriptionProgRequest` (body JSON, nom+prénom requis, trim), INSERT/UPDATE `ON CONFLICT` met à jour les 5 champs ; admin SELECT enrichi (`COALESCE(pi.nom,u.nom)` + `pays`/`lieu_residence`/`titre`). Frontend : type `InscriptionProgPayload`, `useCentresCulturels.inscrireProgrammation(centre,prog,infos)` + `listerPays` (`/api/pays`), nouveau composant Tailwind pur `CentresCulturelsInscriptionProgrammationModal` (formulaire : nom/prénom préremplis depuis le compte, **sélecteur pays** depuis `/api/pays`, lieu de résidence, titre) ouvert au clic « S'inscrire » (désinscription reste un clic) ; tableau admin des inscrits enrichi (titre/pays/lieu de résidence). **Note** : le modal doit rester hors de la chaîne `v-if/v-else-if` de la page (sinon narrowing Volar cassé + bug runtime).
- **centres culturels & programmations : image illustrative (back-office)** : le formulaire admin de **création/édition d'un centre culturel** et celui d'une **programmation** acceptent désormais une **image illustrative (couverture)** via upload. Le centre avait déjà `culture.centre_culturel.image_couverture_url` (backend complet) — seuls les formulaires admin manquaient. La **programmation** a reçu une nouvelle colonne `culture.programmation_centre.image_couverture_url VARCHAR(500)` (migration idempotente `schemas/08e_culture_programmation_image.sql`, nullable → rétrocompat) propagée dans le modèle public (`PROGRAMMATION_COLONNES`, `ProgrammationCentre`/`ProgrammationResponse`/`to_response`), le modèle admin (`ADMIN_PROG_*_COLONNES` + structs + Create/Modifier Request) et le handler admin (INSERT + `ajouter_champ_str!` UPDATE). Frontend : réutilisation du composant générique **`OpportuniteAfriqueImageUploadField`** (upload JWT via `/api/fiches-pays/contributions/upload-image`, retourne URL relative `/uploads/...`) dans les 4 formulaires admin (`centres-culturels/create|[id]`, `programmations/create|[id]`), types `image_couverture_url` ajoutés (`AdminProgrammation*`/`CreerProgrammationForm`/`CreerCentreCulturelForm`/`ProgrammationAPI`), mapping URL→absolu côté lecture publique (`mapperUrlImage` dans `useCentresCulturels` pour centre + programmations + détail programmation), affichage de l'image sur `ProgrammationCard` (fallback icône calendrier si absente).
- **centres culturels international/local** : un centre culturel porte désormais un **type** (`culture.type_centre_culturel` ENUM `international`|`local`, colonne `type_centre` NOT NULL DEFAULT `'local'`). Migration idempotente `schemas/08d_culture_type_centre.sql` (enum créé via bloc `DO` conditionnel, colonne `ADD COLUMN IF NOT EXISTS` → rétrocompat). La page publique `/centres` est **subdivisée en 2 sections** : centres internationaux d'abord, puis locaux (filtrage côté frontend, computed `centresInternationaux`/`centresLocaux`). Backend : `type_centre::text AS type_centre` ajouté aux `CENTRE_CULTUREL_COLONNES` + `ADMIN_CENTRE_*_COLONNES`, champ propagé dans les structs FromRow/Response (public + admin), INSERT/UPDATE admin avec cast `$n::culture.type_centre_culturel` + helper `valider_type_centre` (rejette toute valeur ≠ international/local). Frontend : `type_centre` dans `CentreCulturelAPI`/`AdminCentreCulturel*`/`CreerCentreCulturelForm`, sélecteur dans les formulaires admin create/[id], colonne badge dans la liste admin. Seed `16_seed_centres_culturels.sql` : Montréal/Paris=international, Abidjan/Dakar=local.
- **secteurs d'opportunités afripulse enrichis** : un secteur (`country_profile.secteur_developpement`) porte désormais, en plus de nom/description : **localité**, **contacts** (`contact_telephone`/`contact_courriel`/`contact_adresse`), **références** (`references_utiles` TEXT — `references` étant réservé SQL), **lien web** (`site_web_url`, validé http/https), **image** (`image_url`). Migration idempotente `schemas/11g_country_profile_secteurs_enrichis.sql` (colonnes nullables, rétrocompat). Même canal de **contribution modéré** que les autres objets afripulse. Backend : `SecteurOpportuniteResponse` + SELECT (afripulse_public), INSERT/UPDATE `appliquer_contribution_afripulse` (admin), snapshot d'édition enrichi, validation `valider_secteur` (nom requis + `valider_lien_web` http/https factorisé avec les sites). **Correctif** : `obtenir_snapshot_afripulse` ajoutait `AND deleted_at IS NULL` à toutes les tables, ce qui cassait l'édition/suppression de secteur (table **sans** `deleted_at`) → flag `a_deleted_at` par table. Frontend (Tailwind pur) : type `SecteurOpportuniteAPI` étendu, `ContributionModal` (champs localité, contacts, références, site web `<input type=url>`, image via `ImageUploadField` ; réutilise les champs `formAfripulse` contact_*/site_web_url/image_url + nouveaux `localite`/`references_utiles` ; payload/reset/prefill), `SecteursOpportunitesSection` refondu (carte flex image + contenu : image, localité, description, références, bloc contacts + « Visiter le site web ») + `donnees_actuelles` d'édition enrichi.
- **sites touristiques afripulse : 5 photos + lien web** : un site touristique (`/opportunite-afrique/[id]`) accepte désormais **jusqu'à 5 photos** (au lieu de 3) et un **lien de site web officiel** (`site_web_url`, facultatif). La galerie réutilise la colonne existante `country_profile.site_touristique.images TEXT[]` et l'**endpoint d'upload modéré existant** (`/api/fiches-pays/contributions/upload-image`) — seul le plafond change. Migration idempotente `schemas/11f_country_profile_sites_photos_lien.sql` : `ADD COLUMN site_web_url TEXT` + remplacement de la contrainte `chk_site_images_max3` → `chk_site_images_max5` (`cardinality(images) <= 5`). **Modération inchangée** : photos et lien transitent par le JSONB de contribution, appliqués à l'approbation admin. Backend : plafond `images_site_field` `.take(3)`→`.take(5)`, `site_web_url` validé **http/https uniquement** dans `valider_site_touristique` (rejet `javascript:` etc.), propagé dans l'INSERT/UPDATE `appliquer_contribution_afripulse`, le snapshot d'édition, `SiteTouristiqueResponse` + `SITE_TOURISTIQUE_SELECT` (afripulse_public), et le chemin admin direct (`AdminSiteTouristiqueResponse`/`CreerSiteTouristiqueRequest`/`ModifierSiteTouristiqueRequest` + handlers `creer`/`modifier`/`lister_sites_touristiques`, cap 5). Frontend (Tailwind pur) : type `SiteTouristiqueAPI.site_web_url`, `ContributionModal` (champ `<input type="url">` + `:max="5"` sur `MultiImageUploadField` déjà générique + payload/reset/prefill), `snapshotSite` (SitesTouristiquesSection). **Refonte UI carte → modal** : `SiteTouristiqueCarte` rendu **compact** (couverture cliquable + nom + localisation/gestionnaire + info courte + ligne d'actions « Détails / Modifier / Supprimer ») ; tous les détails (galerie complète à navigation, description, **lien « Visiter le site web »** `target="_blank" rel="noopener noreferrer"` icône `arrow-up-right-from-square`, contacts, GPS, constitution légale, avis) déplacés dans un nouveau composant **`SiteTouristiqueDetailModal`** (Teleport `body`, fermeture Échap/clic-hors-zone, réutilise `OpportuniteAfriqueSiteAvisListe`).
- **fenêtres flottantes déplaçables + redimensionnables** : la fenêtre de `MessagerieFlottante.vue` **et** la salle d'appel direct `AppelDirectSalle.vue` se **déplacent** (glisser la barre de titre) et se **redimensionnent** (poignée coin bas-droit). Positionnement libre en pixels via `:style` réactif (fini le `fixed inset-0` / `fixed bottom-24 right-6 w-… h-…`), borné au viewport et **persisté** dans `localStorage` (clés `messagerie:fenetre`, `appel:fenetre`). Nouveau composable générique **`useFenetreFlottante`** (pointer events + `setPointerCapture`, idiome `AfrolangRoom` ; placement initial bas-droit, recadrage au resize navigateur, min paramétrable). Le drag ignore les clics sur un `<button>` de l'en-tête ; la poignée n'empiète pas sur les contrôles (bouton « envoyer » rounded-full hors hit-testing ; contrôles d'appel centrés). La salle d'appel n'est donc plus plein écran : elle flotte au-dessus de la page (z-[80]).
- **appel direct entre amis (visio sans rendez-vous)** : depuis l'onglet **« Membres »** de la messagerie flottante, on peut **appeler immédiatement un ami** en visioconférence, sans passer par la prise de rendez-vous. L'onglet liste désormais **les amis en tête** (section « Mes amis » avec boutons **Appeler** + **Message**), puis l'annuaire général **sans les amis** (anti-doublon). Réutilise l'infra P2P du rendez-vous (WebRTC/PeerJS, `peer_id` déterministe, anti-glare plus-petit-UUID, `iceServers`/`peerjs*` du runtime config) et le flux **SSE** de la messagerie pour la sonnerie temps réel. **Aucune persistance** : un appel est éphémère, conservé dans un **registre en mémoire** `services::appels::RegistreAppels` (HashMap + TTL 2 min, purge paresseuse, mono-instance comme `RegistreSse`, enregistré en `app_data`). L'**UUID d'appel** sert de secret partagé pour dériver les peer-id (comme l'UUID de rendez-vous). Endpoints `/api/appels` (JWT) : `POST` (démarrer → sonnerie SSE `appel_entrant` au destinataire + config P2P à l'appelant), `GET /{id}/salle` (rejoindre = accepter → SSE `appel_accepte`), `POST /{id}/refuser` (SSE `appel_refuse`), `POST /{id}/annuler` (abandon avant connexion → SSE `appel_annule`). Revérif **amitié active + non bloqué** à chaque action. Backend `models/appel.rs` (`SalleAppelResponse` aligné sur `SalleResponse`, évènements `evt_appel_*`) + `handlers/appels.rs` (réutilise `obtenir_membre_light`, `paire_canonique`, `peer_id`). Frontend : composable `useAppels` (état global `appelEntrant`/`appelActif`/`finAppel`/`appelRepondu`, dispatch SSE), composants Tailwind pur `social/AppelDirectSalle` (salle visio P2P, clone allégé de `RendezVousSalle`) + `social/AppelEntrantPrompt` (sonnerie : carte flottante + bip Web Audio + vibration), montés **globalement** dans `MessagerieFlottante.vue`, `AnnuaireMembres.vue` étendu (section amis + appel), dispatch `appel_*` dans `plugins/messagerie.client.ts`. **Partage d'écran** : bouton dans la salle (`fa-display`, actif une fois connecté) qui échange la piste vidéo envoyée (caméra↔écran) via `getDisplayMedia()` + `RTCRtpSender.replaceTrack()` sur la **même connexion PeerJS** (`mediaConnection.peerConnection`, **sans renégociation** ni signalisation supplémentaire — le pair voit le basculement de façon transparente) ; arrêt via le bouton ou l'UI native du navigateur (`track 'ended'`), restauration de la caméra, vidéo distante en `object-contain` (écran non rogné), badge « Votre écran » sur l'aperçu local.
- **afrolang modération multi-modérateurs + passation par consentement** : une session publique peut avoir **plusieurs modérateurs simultanés** (source = `afrolang.session_participant.role_session='moderateur'`). Le premier utilisateur lambda qui démarre devient modérateur **« placeholder »** (`session.moderateur_id` = ce placeholder non-office, sinon `NULL`). Quand un **modérateur attitré** (désigné au back-office, table `salle_moderateur`) entre, une **demande de passation** s'ouvre : le placeholder reçoit un prompt (« le rôle va changer, acceptez ») et **redevient participant** à l'acceptation ; sans réponse, **promotion automatique après 60 s**. Les **admins plateforme/salle** sont modérateurs **« quoi qu'il arrive »** (immédiat, sans demande, coexistent). Échéance ancrée sur une colonne **stable** `session.demande_passation_at` (insensible aux reconnexions, contrairement à `rejoint_at`) + `demande_passation_par`, migration idempotente `schemas/08c_afrolang_passation_moderation.sql`. **Pas de cron** : résolution **paresseuse** (`resoudre_passation_si_due`, modèle `cloturer_si_necessaire`) en tête des lectures/arrivées/départs, + endpoints explicites `POST /api/afrolang/sessions/{id}/passation/{accepter,finaliser}` (helpers transactionnels `FOR UPDATE` anti-race). Autorisation unifiée `est_moderateur_actif` (office indépendant du rôle ; attitré **gaté** sur `role_session='moderateur'` ; nouvelle variante `NiveauModerateur::Demarreur` — pas de spotlight/fermeture pour abus) — tous les call-sites `moderateur_id` migrés (token `is_moderator`, `can_publish_data`, whiteboard, transferer→co-modérateur). `lister_moderateurs_office` reflète les modérateurs **réellement présents**. Temps réel : DataPackets LiveKit `moderation.{passation_demande,passation_resolue,moderateur_ajoute,moderateur_retire}` ; `passation_en_attente` exposé dans `GET /sessions/{id}` (filet si DataPacket perdu) ; notif cloche `MODERATION_DEMANDE_PASSATION`. Frontend : `useAfrolang` étendu (`demandePassation`, `suisJeModerateur`, `reinitialiserEtatModeration`, `accepter/finaliserPassation`, listener étendu re-fetch autoritaire), `AfrolangRoom` (`estModerateurEffectif` réactif = set serveur, reset au montage/démontage, badges via `moderateursOffice`), nouveau composant Tailwind pur `AfrolangPassationModerationPrompt` (prompt placeholder + bannière désigné + décompte). **Back-office** `/admin/afrolang/moderateurs` corrigé : liste des salles (lecture `data.data`) + **picker recherche utilisateur + select** (fin de l'UUID brut, réutilise `useAdminUtilisateurs`).
- **événements streaming direct** (`001-evenements-streaming`) : un événement **« en ligne » / « hybride »** (`etat='publie'`) peut être diffusé **en direct sur la plateforme** selon un modèle **webinaire** — l'organisateur (= `cree_par`) diffuse caméra/micro/écran, les inscrits regardent et interagissent. Réutilise l'infra **LiveKit** (SFU + token JWT signé backend) déjà câblée pour afrolang ; **aucun média stocké** (pas d'enregistrement). Différence clé avec afrolang : le **token porte `can_publish:false` pour les spectateurs** (D2). Chat / réactions / lever-la-main circulent en **DataPackets LiveKit éphémères** ; seul `main_levee` est reflété en base (liste de demandes fiable pour l'organisateur). Schéma `media_content` : tables `evenement_session` (états `en_cours`/`terminee`, index unique partiel `WHERE etat='en_cours'` = une seule session active, `arret_securite_at` figé à l'ouverture) + `evenement_session_participant` (`role` ∈ `organisateur`/`intervenant`/`spectateur`, `main_levee`), migration idempotente `schemas/09b_media_content_evenements_streaming.sql`. `statut_direct` (`indisponible`/`en_attente`/`en_direct`/`termine`) **dérivé à la lecture**, **pas de cron** : arrêt de sécurité (= `fin+2h`) et cascade d'annulation (FR-016, via `changer_etat_evenement` admin) appliqués **paresseusement** (`cloturer_si_necessaire`). Endpoints sous `/api/evenements/{id}/direct` (JWT) : `GET` (état dérivé + `demandes_parole` si organisateur), `POST /rejoindre` (open-or-join, token scopé, capacité 409 D8, audit `OUVRIR` + notifs inscrits), `/quitter`, `/cloturer` (organisateur), `/lever-main` (spectateur, toggle), `/participants/{uid}/{promouvoir,retrograder,retirer}` (organisateur). Service `livekit_moderation` étendu : `update_participant_can_publish` + `retirer_participant`. Audit `media_content/evenement_session` (`OUVRIR`/`CLOTURER`/`PROMOUVOIR`/`RETROGRADER`/`RETIRER`, **sans** contenu chat/média). Notifs cloche `evenement_direct_demarre` + SSE `{type:"event_stream_demarre", evenement_id}` (flux messagerie existant, dispatch `event_stream_` dans `plugins/messagerie.client.ts`). Backend `models/evenement_streaming.rs` (calculs purs testés) + `handlers/evenement_streaming.rs`. Frontend : `useEvenements` étendu (état direct via `useState` signal SSE), composants Tailwind pur `evenements/EvenementDirectRoom`/`EvenementDirectControls`/`EvenementDirectChat`/`EvenementDirectReactions`/`EvenementDirectModerationPanel`, page plein écran `evenements/[id]/direct.vue`, boutons « Ouvrir/Rejoindre le direct » sur `evenements/[id].vue`. Repli `lien_en_ligne` si LiveKit KO (FR-023). Aucune config LiveKit côté frontend (URL/token fournis par le backend).
- **rendez-vous visio** (`001-rendez-vous-visio`) : deux membres **amis** organisent et tiennent un entretien vidéo 1-à-1. Le backend orchestre uniquement la prise de rendez-vous (proposer → accepter/refuser/contre-proposer → annuler) ; la visioconférence est **pair-à-pair (WebRTC via PeerJS, dép. frontend `peerjs`)** — aucun média ni signalisation applicative ne transite par le serveur (peer-id déterministe `uafr-{sha256(rdv_id‖participant_id)}`, anti-glare = plus petit UUID appelle, STUN public sans TURN, repli messagerie). Schéma `social` : enum `social.statut_rendez_vous` (`propose`/`accepte`/`refuse`/`annule`) + table `social.rendez_vous` (`tour_id` = partie devant répondre, bascule à chaque contre-proposition ; `duree_minutes ∈ {15,30,45,60}` ; soft delete), migration idempotente `schemas/31_social_rendez_vous.sql`. « expiré »/« terminé » **dérivés par calcul** (statut+date+durée), pas de cron. Endpoints `/api/rendez-vous` (JWT) : `POST` (proposer), `GET` (lister, filtres `attente_moi`/`attente_autre`/`a_venir`/`passes`), `GET /{id}`, `GET /{id}/salle` (config P2P, fenêtre `[date−5min, date+durée+15min]`), `POST /{id}/{accepter,refuser,contre-proposer,annuler}` (verrouillage optimiste 409). Revérif amitié/blocage à chaque action (FR-034), audit `social/rendez_vous` **sans** sujet/description (FR-033), notifications cloche unifiée (`creer_notification`, types `rdv_*`) + SSE (`{type:"rdv_*", rendez_vous_id}`) via le flux messagerie existant. Backend `models/rendez_vous.rs` + `handlers/rendez_vous.rs`. Frontend : composable `useRendezVous`, composants Tailwind pur `social/RendezVousProposerModal`/`RendezVousCarte`/`RendezVousContreModal`/`RendezVousListe`/`RendezVousSalle`, 3ᵉ onglet « Rendez-vous » (pastille) dans `MessagerieFlottante.vue`, bouton sur `profil/[id].vue`, dispatch `rdv_*` dans `plugins/messagerie.client.ts` (+ refresh cloche). Config runtime `nuxt.config.ts` : `peerjsHost/Port/Path/Secure`, `iceServers` (surchargeables `NUXT_PUBLIC_*`).
- **vidafrica contributions membres** : tout utilisateur connecté peut, depuis `vidafrica/[slug].vue`, **proposer une vidéo** (upload, créée en `etat='brouillon'`) et **contribuer une piste de sous-titres** (piste en `brouillon`, segments + tap-to-mark karaoké), accessible aussi via le bouton « Proposer une vidéo » de `vidafrica/index.vue`. Modération **au niveau de la piste** : nouvelle colonne `media_content.piste_sous_titre.etat` (`brouillon`|`publie`|`masque`, DEFAULT `publie`, migration idempotente `schemas/27b_vidafrica_contributions_membres.sql`). Les lectures publiques (`obtenir_sous_titres`, langues disponibles) ne montrent que les pistes `publie`. Handler membre `src/handlers/vidafrica_contribution.rs` (auth JWT in-handler comme le marché membre, contrôle de propriété + piste `brouillon` requis) exposé sous `/api/vidafrica/*` (POST `/videos`, `mes-pistes`, `/videos/{id}/pistes`, segments, timings-mot). Côté admin : `PATCH /api/admin/vidafrica/pistes/{id}/etat` (`changer_etat_piste`) pour valider/masquer ; la liste des pistes admin expose désormais `etat` + `cree_par_nom`. Frontend : composable `useVidafricaContribution`, composants Tailwind pur `VidafricaProposerVideoModal`/`VidafricaTapToMarkPublic`/`VidafricaContribuerSousTitres`, composable admin `useAdminVidafrica.changerEtatPiste`.
- **vidafrica sous-titres collaboratifs** : assouplissement du modèle de pistes. **(1)** plusieurs membres peuvent proposer leur propre piste dans la **même langue** (unicité désormais **par auteur** : un membre = une piste par langue), mais **une seule piste publiée par langue** (l'admin arbitre). **(2)** l'auteur peut **éditer sa piste après publication** (modifications en direct, sans re-validation) ; seules les pistes `masque` sont gelées. **(3)** l'**auteur du sous-titrage** est affiché sous la vidéo publique (déresponsabilisation plateforme). Migration idempotente `schemas/27e_vidafrica_pistes_collaboratif.sql` : `DROP idx_piste_sous_titre_unique_langue` → `idx_piste_sous_titre_unique_langue_auteur` UNIQUE `(video_id, langue, cree_par)` + `idx_piste_sous_titre_unique_publie` UNIQUE `(video_id, langue) WHERE etat='publie'`. Backend : `verifier_piste_modifiable` autorise `publie` (bloque `masque`), `creer_piste_membre`/`mes_pistes` scopés à `cree_par`, `changer_etat_piste` (admin) garde « 0 segment → refus de publier » + « une seule publiée/langue → refus, masquer l'autre d'abord », `obtenir_sous_titres` renvoie `auteur` (LEFT JOIN `iam.utilisateur`), `obtenir_video_publique.langues_disponibles` filtre `etat='publie'`. Frontend : `useVidafrica` (`SousTitres.auteur`), mention auteur sous le lecteur dans `vidafrica/[slug].vue`, `VidafricaContribuerSousTitres` (`pisteEditable` = ≠ `masque`, suppression/édition possibles après publication).
- **factcheck volets + réactions** : chaque factcheck porte deux volets 1:1 (préjugé/réalité) avec titre/description et compteur de cœurs indépendant (`prejuge_*`, `realite_*` sur `governance.factcheck`). Réaction globale au post = jeu d'emojis (cœur, pouce, rire, je n'aime pas) une par utilisateur. Table `governance.factcheck_reaction` étendue avec `cible` (`general`|`prejuge`|`realite`), `type_reaction` élargi, `UNIQUE(factcheck_id, utilisateur_id, cible)`. Migration `schemas/10f_governance_factcheck_volets_reactions.sql` (idempotente). Endpoint `POST /api/gouvernance/factcheck/{id}/reaction` (toggle, JWT requis) ; `lister_contributions` enrichi (volets + compteurs + `ma_reaction`/`a_like_*` via JWT optionnel — `getContributions` envoie le header auth) ; `creer_factcheck_public` + `FactCheckCreateModal` saisissent les volets. Page `factcheck.vue` branchée sur l'API (hors mocks), composable `useGouvernance.reagir(id, cible, type)`.
- **factcheck signalement (modération communautaire)** : bouton « Signaler » sur chaque carte ; un signalement par utilisateur (`UNIQUE`). Au-delà de **20 signalements distincts** (`SEUIL_SIGNALEMENTS_SUSPENSION`), le factcheck passe automatiquement en `etat='suspendu'` (retiré de la liste publique). Table `governance.factcheck_signalement` + colonne `nombre_signalements`, migration `schemas/10g_governance_factcheck_signalement.sql` (idempotente). Endpoint `POST /api/gouvernance/factcheck/{id}/signalement` (JWT requis, insert idempotent + recompte + suspension). `lister_contributions` expose `a_signale`. Composable `useGouvernance.signaler(id, motif?)`. Audit `SIGNALEMENT` instrumenté.
- **marché membre** (`001-marche-achat-vente-troc-don`) : couche d'endpoints membre `/api/annonces` (auth JWT) pour publier (multipart, photos `marketplace/annonces/`, `etat='publiee'` immédiat), gérer (« Mes annonces », modifier, conclure, supprimer soft), favoris, et contacter l'auteur via la **messagerie existante** (`social.conversation.annonce_id`, `30_social_conversation_annonce.sql`). Enum `etat_annonce` étend `'conclue'`. `envoyer_message` assoupli (amitié OU conversation existante, D2). Endpoint public `/api/annonces/categories`. Validation photos `image_validation::valider_photo_annonce` (JPEG/PNG/WebP, 3 Mo). Composable `useMarcheAfricain` étendu, composants `MarcheAnnonceForm.vue`/`MarcheFavoriBouton.vue`, pages `mes-annonces.vue`/`favoris.vue`.
- **social** (`001-demande-amitie`) : amitié entre membres + messagerie privée temps réel (SSE) via bouton flottant global. Schéma `social` (`schemas/29_social.sql`).
- **expertise** (`001-demande-expertise`) : candidature `/devenir-expert`, validation admin + email. Schéma `iam.expertise` étendu.
- **annuaire-membres** : `/profil` = annuaire public, `/profil/[id]` = page de détail unifiée (membre + biblio + expertise).
- **bibliothèques humaines** (`001-admin-biblio-humaine`) : workflow admin de validation. Schéma `iam` (`04b_iam_biblio_demande.sql`).
- **afrolang** : refonte salles (streaming public + privées par code bcrypt), pays d'origine, propositions communautaires + admins de salle, modération de session (permissions tableau blanc + spotlight), fermeture admin + historique modération, migration tableau blanc tldraw → Excalidraw.
- **afripulse** (`001-afripulse-contributions`) : enrichissement collaboratif fiches pays `/opportunite-afrique`. Schéma `country_profile` étendu (`11c_country_profile_afripulse.sql`).
- **sites touristiques enrichis** (`001-sites-touristiques-enrichis`) : sous-type (enum `sous_type_site`, 20 valeurs, validation famille↔sous-type en code), fiche complète (gestionnaire, localisation, GPS, info pertinente, contacts publics), constitution légale facultative, badge « Vérifié » admin (audité), avis visiteurs notés 1–5 (table `avis_site`, upsert écriture directe + modération admin). Schéma `country_profile` étendu (`11d_country_profile_sites_enrichis.sql`). Endpoints publics `/api/sites-touristiques/{id}/avis` + admin vérification/masquage. Composants `SiteTouristiqueCarte.vue`, `SiteAvisListe.vue`.
- **infos pratiques voyage** : bloc « À savoir avant de voyager » → section structurée (8 champs scalaires sur `country_profile.fiche_pays` : `voyage_langue_internationale`, `voyage_langue_locale`, `voyage_infos_visa`, `voyage_infos_sanitaires`, `voyage_meteo`, `voyage_prises_electriques`, `voyage_contacts_tourisme`, `voyage_recommandations_securite`). Schéma `11e_country_profile_infos_voyage.sql`. Contribuable par tous via le canal scalaire `fiche_pays` existant (`SECTIONS_VALIDES` + `appliquer_fiche_scalaire`, modération admin). UI : mode « champ ciblé » du `ContributionModal.vue`, bloc dans `SavoirAvantVoyagerSection.vue`.
- **vidafrica** (`004-vidafrica-sous-titres`) : vidéos + sous-titres. Schéma `media_content` étendu.
- **arbre généalogique** (`001-personnes-arbre` et suivantes) : schéma `arbre_genealogique`, visualisation @vue-flow, édition, matching inter-arbres (pg_trgm), recherche, collaboration/partage, notifications/suggestions.

## Active Technologies
- Rust Edition 2024 (backend), TypeScript / Nuxt 4 (Vue 3 SSR) (frontend) + Actix-Web 4, sqlx (PostgreSQL), uuid, chrono, serde, sanitize-filename, image, lettre (backend) ; Pinia, Tailwind CSS v4, FontAwesome (frontend) (001-sites-touristiques-enrichis)
- PostgreSQL 16, schéma `country_profile` (source de vérité — Principe III) (001-sites-touristiques-enrichis)
- Rust Edition 2024 (backend) ; TypeScript / Nuxt 4 (Vue 3 SSR) (frontend) + Actix-Web 4, sqlx (PostgreSQL), actix-multipart, `image` crate, service interne `image_validation`, JWT (`jwt.rs`), `audit::log_action`, SSE (`messagerie_sse::RegistreSse`) ; Pinia, Tailwind CSS v4 (pur), FontAwesome (frontend) (001-marche-achat-vente-troc-don)
- PostgreSQL 16, schémas `marketplace` (source de vérité — Principe III) et `social` (messagerie). Upload photos en local sous `./uploads/marketplace/annonces/` servi par actix-files (001-marche-achat-vente-troc-don)
- Rust Edition 2024 (backend), TypeScript / Nuxt 4 (Vue 3 SSR) (frontend) + Actix-Web 4, sqlx (PostgreSQL), uuid, chrono, serde, `audit::log_action`, `RegistreSse` (SSE) (backend) ; Pinia, Tailwind CSS v4 (pur), FontAwesome, **peerjs** (WebRTC P2P) (frontend) (001-rendez-vous-visio)
- PostgreSQL 16, schéma `social` (source de vérité — Principe III). Cloche persistante via `arbre_genealogique.notifications` (système cloche unifié existant). Aucun stockage de média (P2P). (001-rendez-vous-visio)
- Rust Edition 2024 (backend) ; TypeScript / Nuxt 4 (Vue 3 SSR) (frontend) + Actix-Web 4, sqlx (PostgreSQL), `livekit-api` 0.4, `livekit-protocol` 0.7, `jsonwebtoken`, `uuid`, `chrono`, `serde` (backend) ; `livekit-client` ^2.17.1 (déjà installé), Pinia, Tailwind CSS v4 (pur), FontAwesome (frontend) (001-evenements-streaming)
- PostgreSQL 16, schéma `media_content` (source de vérité — Principe III). **Aucun stockage de média** (flux via SFU LiveKit). Cloche persistante via `arbre_genealogique.notifications` (système unifié existant). (001-evenements-streaming)
